Maybe i could rewrite the memory buffers in therms of allocators, dynamic and
static vectors to make the interface independent of the memory layout. Maybe
implement Expression templates for them also
